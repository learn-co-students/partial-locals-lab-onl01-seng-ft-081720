Classroom Info
<p>
<%= render partial: "classroom", locals: {classroom: @classroom} %>

<ul>
<% @classroom.students.each do |student| %>
    <li><%= render partial: "students/student", locals: {student: student} %></li><br>
<% end %>
</ul>

</p>
